# Description

What and why. Link the issue if one exists (`closes #123`).

## Checklist

- [ ] `ruff check .` and `pytest -q` pass locally (same as CI).
- [ ] Default behavior is unchanged: paper runs still reproduce, and new
      training-loop behavior is behind an off-by-default flag.
